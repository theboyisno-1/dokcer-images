{
  "name": "nodejs-env",
  "version": "1.0.0",
  "description": "With this nodejs + express server we check values for any environment variable available to it. In order to access variable value hit `localhost:3000/get/<NAME-OF-ENV-VAR>`",
  "main": "index.js",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Abdul Khan",
  "license": "ISC"
}
